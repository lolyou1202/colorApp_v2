//import chroma from 'chroma-js'

//export const useScaleGradient = (
//	startColor: string,
//	endColor: string,
//	numOfPieces: number
//): string[] => {
//	const scale = chroma.scale([startColor, endColor])

//	const colorList = Array.from({ length: numOfPieces }).map((_, index) =>
//		scale(index / (numOfPieces - 1))
//			.hex()
//			.toUpperCase()
//	)

//	return colorList
//}
